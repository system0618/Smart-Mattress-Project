# 邵博儒 AI 协同开发记录

## 记录格式

```markdown
## YYYY-MM-DD

### 使用工具

### 提问或任务

### AI 输出摘要

### 采纳内容

### 人工修改与验证
```

## 2026-09-10

### 使用工具

- Codex：读取项目要求、参考论文和现有仓库代码。
- Python：读取 `data.json`、检查数据形状、训练和评估模型。
- PyTorch：训练压力热力图 CNN。
- scikit-learn：实现 SVM、随机森林和分类指标评估。
- Git：提交睡姿识别模块代码。

### 提问或任务

根据专业方向实践要求和参考论文，完成睡姿识别算法及功能性要求。用户提出两条思路：传统机器学习手工特征基线，以及把压力热力图作为图像输入的小型 CNN。随后明确只完成睡姿识别部分，并要求继续项目、安装依赖、提交 Git，最后将本次对话记录写入本文件。

### AI 输出摘要

1. 阅读实践要求和《基于柔性压力传感器的睡姿识别方法研究_刘今越.pdf》。确认课程要求至少实现两种睡姿识别算法，并输出准确率、Precision、Recall、F1 等指标。
2. 检查项目数据后，确定区域划分数据的 `data.json` 含有 `44 x 24` 压力矩阵、用户信息、动作信息和 `sleep_pos` 标签。
3. 实现两种主要算法：
   - 手工压力特征 + RBF-SVM，并保留随机森林作为可选对照；
   - 直接将 `44 x 24` 压力热力图作为单通道图像输入的轻量 CNN。
4. 实现按用户划分的 70% 训练集和 30% 测试集，避免同一用户的数据同时出现在训练集和测试集。
5. 实现压力归一化、动作开始帧过滤、空载帧过滤、噪声和增益扰动增强，以及 Accuracy、Precision、Recall、F1 和混淆矩阵输出。
6. 当前数据实际包含 4 类标签 `0..3`，模型按实际类别数训练，并保留最多扩展到 6 类的结构。
7. 安装 PyTorch、joblib 和 scikit-learn，完成 30 轮 CNN 训练。
8. 在 3,942 个未见用户测试样本上获得：Accuracy 98.66%，Macro Precision 98.66%，Macro Recall 98.71%，Macro F1 98.66%。
9. 创建 Git 提交：`d1ae344 feat: implement sleep posture recognition`。

### 采纳内容

- 采用压力热力图直接图像分类的 CNN 作为主算法。
- 采用手工特征 + RBF-SVM 作为传统机器学习基线。
- 采用用户级数据划分作为最终测试方案。
- 将算法说明和功能性要求写入 `src/posture_recognition/算法与功能性要求.md`。

### 人工修改与验证

- 修改 `src/posture_recognition/train.py`，完成训练入口和 CNN/SVM/随机森林训练逻辑。
- 修改 `src/posture_recognition/evaluate.py`，完成统一评估逻辑。
- 新增 `dataset.py`、`features.py`、`models.py`。
- 更新 `src/posture_recognition/README.md`。
- 通过 `python -m compileall -q src/posture_recognition` 语法检查。
- 验证真实数据形状为 `(13140, 44, 24)`，训练集 9,198 帧，测试集 3,942 帧，训练测试用户无交集。
- 生成本地模型 `src/posture_recognition/models/cnn.pt`、划分信息和测试指标文件。
- 未修改身体部位划分、压力增强和可视化模块。

## 本次对话记录（按时间顺序）

### 用户

根据 `G:\专业实践\专业方向实践-2026秋季-202608302000.pdf`，参考论文 `G:\专业实践\基于柔性压力传感器的睡姿识别方法研究_刘今越.pdf`，完成睡姿识别算法及其功能性要求这部分。我的想法是算法 A：传统机器学习基线，输入不直接用整张矩阵，而用手工特征；参考刘今越那篇论文的做法。算法 B：小型 CNN，把压力热力图当图像分类。

### AI

读取实践要求、参考论文和项目代码，确认课程要求至少实现两种睡姿识别算法，并输出准确率、Precision、Recall 和 F1。检查项目后发现现有睡姿识别代码仍是占位实现，数据中可以使用带 `sleep_pos` 标签的压力矩阵数据。

### 用户

当然你也可以使用别的算法，只要好用就行，这只是我的想法。

### AI

确定保留传统机器学习作为基线，同时实现直接利用压力热力图空间结构的轻量 CNN；如果数据更适合其他模型，可将其作为可选方案。随后确认 `data.json` 中每条记录包含压力数据、用户、动作和睡姿标签。

### 用户

只做睡姿识别这一部分就行，其他的不要动。

### AI

将修改范围限定为 `src/posture_recognition/`，不修改身体部位划分、可视化、压力增强和其他扩展模块。确认数据压力帧尺寸为 `44 x 24`，标签字段为 `sleep_pos`，并按用户进行 70%/30% 划分。

### 用户

把压力热力图当图像分类。

### AI

明确将 CNN 作为主算法：把每帧 `44 x 24` 压力矩阵归一化为单通道图像，经过卷积、池化、全局平均池化和全连接层完成睡姿分类。传统 SVM/随机森林仍作为对照算法保留。

### AI 实现内容

新增和修改以下内容：

- `dataset.py`：读取标注压力数据，并执行用户级训练测试划分。
- `features.py`：提取压力均值、方差、压力重心、区域范围、左右压力差、行列投影等手工特征。
- `models.py`：实现轻量 `TinyPostureCNN`。
- `train.py`：实现 CNN、RBF-SVM 和随机森林训练入口。
- `evaluate.py`：实现准确率、Precision、Recall、F1 和混淆矩阵评估。
- `README.md`：补充运行方式和算法说明。
- `算法与功能性要求.md`：补充睡姿识别任务定义、CNN 结构、评价指标和功能要求。

### 用户

安装。

### AI

安装 PyTorch 和 joblib。第一次安装因系统盘空间不足失败，清理 pip 可再生成缓存后，将临时安装目录切换到工作盘重新安装。随后成功安装：

- PyTorch `2.14.0+cpu`
- joblib `1.6.0`
- scikit-learn `1.9.0`

完成 CNN 前向推理检查和代码语法检查。

### 用户

继续项目进行。

### AI

启动真实数据训练。期间修复了训练入口的命令行参数映射问题，并继续训练 CNN。当前 JSON 数据实际包含 4 个有效类别标签 `0..3`，因此模型按照实际类别数训练，同时保留扩展到最多 6 类的能力。

训练配置：

- 输入：`44 x 24` 压力热力图；
- 训练样本：9,198 帧；
- 测试样本：3,942 帧；
- 划分方式：用户级 70%/30%；
- 训练轮数：30；
- 设备：CPU。

测试结果：

- Accuracy：`98.66%`；
- Macro Precision：`98.66%`；
- Macro Recall：`98.71%`；
- Macro F1：`98.66%`。

生成模型和结果文件：

- `src/posture_recognition/models/cnn.pt`；
- `src/posture_recognition/models/cnn_split.json`；
- `src/posture_recognition/models/cnn_test_metrics.json`。

### 用户

总结一下你干了什么工作，生成了什么内容。

### AI

总结完成睡姿识别模块、CNN 图像分类、SVM/随机森林基线、用户级数据划分、评价指标和功能性要求文档，并说明测试准确率为 98.66%。

### 用户

睡姿识别的两种算法分别是什么。

### AI

说明两种主要算法：

1. 手工压力特征 + RBF-SVM；
2. 压力热力图 + 轻量 CNN 图像分类。

同时说明随机森林是传统算法的可选对照实现。

### 用户

将本次对话记录写在 `G:\专业实践\Smart-Mattress-Project\ai_logs\shaoboru_ai_log.md` 里面。

### AI

按照项目已有日志模板，将本次工作总结追加到 `ai_logs/shaoboru_ai_log.md`，内容包括工具、任务、算法、训练结果、生成文件、验证过程和 Git 提交信息。

### 用户

将本次对话的所有内容复制一份在 `G:\专业实践\Smart-Mattress-Project\ai_logs\shaoboru_ai_log.md` 下面。

### AI

在已有总结记录下方追加本段按时间顺序整理的对话记录，保留原有日志内容不变。
