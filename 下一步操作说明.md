# 下一步操作说明

本地 Git 仓库已经创建并提交完成。下面这些步骤需要你使用自己的 GitHub 账号完成。

## 必做步骤

1. 打开 GitHub，创建空仓库 `Smart-Mattress-Project`。
2. 不要勾选 GitHub 页面上的 README、`.gitignore` 或 License。
3. 复制新仓库页面给出的 HTTPS 地址，例如：

```text
https://github.com/你的用户名/Smart-Mattress-Project.git
```

4. 在 PowerShell 中执行：

```powershell
cd E:\workplace\Smart-Mattress-Project
.\scripts\publish_to_github.ps1 -RemoteUrl "https://github.com/你的用户名/Smart-Mattress-Project.git"
```

5. 如果 GitHub 提示登录，按终端或浏览器提示完成授权。
6. 推送成功后，在 GitHub 仓库页面进入 `Settings` -> `Collaborators and teams`，邀请团队成员。

## 推荐设置

- 开启 `Issues`，用于记录任务和问题。
- 创建 `Projects` 看板，用于课程过程管理。
- 要求成员通过 Pull Request 合并到 `main`。
- 课程提交前执行：

```powershell
git log --oneline --decorate --graph --all > docs/project_management/git_log.txt
```

更多细节见 [docs/github_shared_repo_guide.md](docs/github_shared_repo_guide.md)。

## 待确认

- 仓库设置为 Public 还是 Private。
- 每位成员的 GitHub 用户名。

- 可视化采用 Web、Unity，还是两者并行。
